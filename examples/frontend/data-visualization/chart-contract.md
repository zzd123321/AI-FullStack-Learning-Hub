# 趋势图组件契约

- 输入只接受有限数值；缺失值、时区和单位必须在领域适配层显式处理。
- 数据按时间升序显示；相同时间戳的合并规则由调用方决定。
- 鼠标、触摸和键盘共享同一份 `InteractionState`，选中态不是临时悬停态。
- Canvas/WebGL 视图必须同时提供简短文字摘要和可访问数据表入口。
- 实时模式只保留最近 `capacity` 个采样点，并按动画帧合并重绘请求。
- 组件卸载时释放观察器、事件监听、动画帧、Worker 与 GPU 资源。
