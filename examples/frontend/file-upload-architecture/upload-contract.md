# 上传系统契约

- 浏览器只提交用户选择的 `File`；文件名、扩展名、MIME 和客户端校验均不可信。
- 后端创建上传会话并决定对象键、分片大小、允许类型、配额和短期上传权限。
- 分片编号和字节范围固定；重试同一分片不能改变其内容。
- 进度表示已传输字节，不代表对象已完成、可读取或业务处理成功。
- 完成接口必须幂等，并只接受属于当前用户和上传会话的分片清单。
- 取消分为“停止本机请求”和“终止服务端会话”；两者不能混为一谈。
- 断点记录不保存预签名 URL，恢复时重新向后端取得短期权限。
- 服务端重新验证大小、内容特征、恶意文件、授权和完整性，再发布资产。
