[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "task-domain-example"
version = "0.1.0"
description = "Runnable example for Python classes, dataclasses, and protocols"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["task_domain"]
