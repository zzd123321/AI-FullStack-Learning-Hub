[project]
name = "python-object-model-basics"
version = "0.1.0"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["object_model_basics*"]
